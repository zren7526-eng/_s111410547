沒問題！為您統整這 10 個練習題的「純題目版本」，非常適合用來當作自我測驗。您可以把它們貼到您的編輯器中，當作註解來輔助您寫程式：

JavaScript 綜合練習題 (1-10)
1. 判斷及格與否

目標： 寫一個函式，傳入一個數字（分數），判斷該分數是否及格（60 分或 60 分以上）。如果及格回傳字串 "及格"，否則回傳 "不及格"。

運用概念： function, if

2. 計算陣列總和

目標： 寫一個函式，傳入一個包含多個數字的陣列，利用迴圈計算並回傳所有數字的總和。

運用概念： function, Array, for

3. 找出第一個偶數

目標： 寫一個函式，傳入一個數字陣列。使用 while 迴圈在陣列中尋找，找到第一個「偶數」就立刻停止迴圈並回傳該數字。如果整個陣列都沒有偶數，則回傳 null。

運用概念： function, Array, while, if

4. 建立與讀取個人資料

目標： 建立一個包含 name (姓名) 和 age (年齡) 兩個屬性的物件。接著寫一個函式，接收這個物件作為參數，並回傳一句介紹詞（例如："你好，我是小明，今年 25 歲。"）。

運用概念： Object, function

5. 解析伺服器回傳的 JSON 格式

目標： 假設有一段 JSON 格式的字串 '{"product": "筆記型電腦", "price": 35000}'。請寫一段程式碼，將它轉換成 JavaScript 可以操作的物件，並印出該物件的 product 屬性值。

運用概念： JSON, Object

6. 將資料轉換成 JSON 傳送

目標： 建立一個包含兩個購物項目的陣列，每個項目都是一個物件（包含 item 品名與 quantity 數量）。請將這個 JavaScript 陣列轉換成 JSON 格式的字串並印出。

運用概念： Array, Object, JSON

7. 篩選成年使用者

目標： 寫一個函式，傳入一個包含多個「使用者物件（含姓名與年齡）」的陣列。利用迴圈與判斷式，挑出年齡大於等於 18 歲的人，將他們放進一個新的陣列中並回傳。

運用概念： function, Array, Object, for, if

8. 購物車計算總金額

目標： 寫一個函式，傳入一個購物車陣列（裡面包含多個物件，每個物件有 name 商品名、price 單價、qty 數量）。計算並回傳這台購物車的總金額（單價乘以數量的加總）。

運用概念： function, Array, Object, for

9. 檢查庫存警告

目標： 給定一個包含多個商品物件（含商品名與庫存量 stock）的陣列。請規定使用 while 迴圈檢查，只要發現有商品庫存小於 5，就印出警告訊息（例如："警告：某某商品 庫存不足！"）。

運用概念： Array, Object, while, if

10. 綜合挑戰：處理學生成績 API 資料

目標： 寫一個函式，接收一段「學生資料的 JSON 字串」（包含多筆學生的姓名與分數）。在函式內將其解析成陣列後，計算出所有及格學生（分數大於等於 60）的平均分數並回傳。如果沒有任何人及格，則回傳 0。

運用概念： function, JSON, Array, Object, for (或 while), if   